for (int i = 0; i < inputtext.Length; i++) {
                    for(int o = 0; o < SearchTextInputText.text.Length; o++){
                        if(SearchTextInputText.text[o] == inputtext[i]){
                            foreach(Transform Button in ButtonArray) {
                                if(Button.GetChild(0).GetComponent<TextMeshProUGUI>().text == inputtext) {
                                    Button.gameObject.SetActive(true);
                                }
                            }   
                        } else {
                            foreach(Transform Button in ButtonArray) {
                                if(Button.GetChild(0).GetComponent<TextMeshProUGUI>().text == inputtext) {
                                    Button.gameObject.SetActive(false);
                                }
                            }
                        }   
                    }
                    
                }